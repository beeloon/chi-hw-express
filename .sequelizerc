const path = require('path');

module.exports = {
  config: path.resolve(__dirname, 'app', 'database', 'config', 'default.cjs'),
  'migrations-path': path.resolve(__dirname, 'app', 'database', 'migrations'),
};
